FROM archlinux:latest

####################
# install packages #
####################

ENV CC=clang
ENV CXX=clang++
ADD ./archlinux/scripts/add-aur.sh /root
ADD ./archlinux/scripts/PKGBUILD /home/build/
RUN bash /root/add-aur.sh "build"
RUN pacman -S --needed --noconfirm clang make

